;; init-ceded -- Configure protobuf-mode

;;; Commentary:
;; Configure protobuf-mode and semantic

;;; Code:

(require 'gen-package)
(require 'use-package)
(use-package protobuf-mode
  :ensure t)


(provide 'plugin-protobuf-mode)
;;; init-protobuf-mode.el ends here
