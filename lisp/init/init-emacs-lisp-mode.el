;;; init-emacs-lisp-mode -- Emacs lisp mode configuration

;;; Commentary:
;; Adds a hook to enable electric-pair mode for all buffers running
;; emacs-lisp-mode

;;; Code:
;;(add-hook 'emacs-lisp-mode-hook #'(lambda () (electric-pair-local-mode)))

(provide 'init-emacs-lisp-mode)
;;; init-emacs-lisp-mode.el ends here
