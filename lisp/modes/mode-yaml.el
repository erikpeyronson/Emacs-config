;;; mode-yaml -- Setup yaml mode

;;; Commentary:
;; install yaml mode and flymd

;;; Code:
(require 'use-package)

(use-package yaml-mode
  :ensure t
  )

(provide 'mode-yaml)
;;; mode-yaml.el ends here
